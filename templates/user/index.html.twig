{% extends 'base.html.twig' %}

{% block title %}Users List{% endblock %}

{% block body %}
    {% if users %}
        <h2>Liste des utilisateurs :</h2>
        <ul>
        {% for user in users %}
            <li>
                <a href="{{ path('user_details', {id: user.id}) }}">{{user.username}}</a>
            </li>
        {% endfor %}
        </ul>
    {% else %}
        <h2>Aucun utilisateur enregistré</h2>
    {% endif %}
    <a href="{{ path('user_signup') }}">Nouvel Utilisateur</a>
{% endblock %}
